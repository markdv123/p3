// const { <Model>,<model> } = require ('../models')

/* functions to be called by the router
const <functionName> = async ( req, resp ) => {
   try {

   }
   catch (err) {
      console.log ( "Error in <functionName>", err)
      throw err
   }
}

   */

module.exports = {
   // <functionName>
}
